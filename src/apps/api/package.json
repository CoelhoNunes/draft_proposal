{
  "name": "microtech-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "tsc -b --clean",
    "build": "tsc -b",
    "dev": "node server.js",
    "start": "node server.js",
    "pretest": "pnpm -w -r build",
    "test": "node --test \"./tests/**/*.test.mjs\""
  },
  "dependencies": {
    "@fastify/cors": "^8.4.0",
    "@fastify/helmet": "^11.1.1",
    "@fastify/multipart": "^8.3.0",
    "@fastify/static": "^6.12.0",
    "@fastify/swagger": "^8.15.0",
    "@fastify/swagger-ui": "^2.1.0",
    "@microtech/ai": "workspace:*",
    "@microtech/core": "workspace:*",
    "dotenv": "^17.2.3",
    "fastify": "^4.24.3",
    "openai": "^4.56.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
